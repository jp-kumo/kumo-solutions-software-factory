# 2026-02-14

## Nightly builder progress
- Upgraded `review_app/frontend/src/App.jsx` to load backend `/api/settings` dynamically and expose a quality profile selector (`best`, `balanced`, `small`) before video download.
- Download API requests now pass selected `quality_profile` from UI, with success notifications reflecting selected profile.
- Transcript action buttons now render from backend-provided `transcript_formats` for backend/frontend consistency.
- Hardened `scripts/review_app_nightly_check.sh` to run syntax checks + frontend production build, with graceful fallbacks when `python3-venv` or backend dependencies are unavailable.
- Updated `review_app/README.md` to document dynamic settings + improved nightly validation workflow.
- Validation run completed successfully for syntax + frontend build (`npm ci && npm run build`); backend unit tests were skipped due missing local backend deps in available virtualenv.
