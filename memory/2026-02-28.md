# 2026-02-28

## Nightly Builder Mode
- Upgraded `scripts/kumo_proposal_generator.sh`:
  - Added `set -euo pipefail` safety
  - Added `--help`
  - Added optional defaults for date/output path
  - Added date format validation (`YYYY-MM-DD`)
  - Added robust placeholder replacement with `perl` (handles special chars in client names)
  - Added automatic slugged filename + `proposals/` output folder creation
- Added docs: `docs/kumo-proposal-generator.md`
- Test run successful:
  - `./scripts/kumo_proposal_generator.sh "Acme / Bio & Labs" "2026-02-28"`
  - Output generated at `proposals/2026-02-28-acme-bio-labs-proposal.md`
