# 2026-03-05

## Nightly Builder Mode (UTC early morning)
- Enhanced `scripts/check_project_markdown_compliance.py`:
  - Added per-project required-doc coverage metrics (`present_required_count`, `required_total`, `required_coverage_pct`).
  - Added `--project-glob` option to target subsets of projects.
  - Updated markdown output table with required coverage column.
  - Included `project_glob` in JSON report metadata.
- Extended `scripts/test_project_markdown_compliance.py` with new test coverage for `project_glob` filtering.
- Ran tests: `python3 scripts/test_project_markdown_compliance.py` (5 tests passing).
- Regenerated compliance reports in `data/` with new coverage column.
